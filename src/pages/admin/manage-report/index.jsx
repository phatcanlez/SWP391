import CRUDTemplate from "../../../components/crud-template";

function Report() {
  const columns = [];

  const formItems = <></>;

  return (
    <CRUDTemplate columns={columns} formItems={formItems} path="/report" />
  );
}

export default Report;
