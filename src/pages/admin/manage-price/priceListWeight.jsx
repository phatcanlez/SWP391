import CRUDTemplate from "../../../components/crud-template";

function PriceListWeight() {
  const columns = [];

  const formItems = <></>;

  return (
    <CRUDTemplate
      columns={columns}
      formItems={formItems}
      path="/pricelistweight"
    />
  );
}

export default PriceListWeight;
