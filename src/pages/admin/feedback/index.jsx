import CRUDTemplate from "../../../components/crud-template";

function Feedback() {
  const columns = [];

  const formItems = <></>;

  return (
    <CRUDTemplate columns={columns} formItems={formItems} path="/feedback" />
  );
}

export default Feedback;
